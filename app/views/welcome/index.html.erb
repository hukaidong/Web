<h2>Hello World</h2>
<p>
  The time is now: <%= Time.now %>
  <%= form_with url:a_number_path, local:true do |f| %>
    <%= f.text_field :a_number,  value: @a_number.as_integer %>
    <%= f.submit "update number" %>
  <% end %>
</p>
